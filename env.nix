{ config, pkgs, lib, ... }:
{
# TODO: This will be the file that describes environment vars and should be gitignored so the whole repo can be public  

# Consider renaming the skeleton version of this to env.template.nix and leaving it unignored so users can fill in the file without the script if they prefer
}

